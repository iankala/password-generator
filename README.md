# 🔐 Password Generator

A browser-based password generator that creates secure, random passwords instantly.

## About

This tool generates strong random passwords directly in the browser using HTML and CSS. No backend, no tracking — everything happens client-side.

## Features

- Instant password generation
- Clean, minimal UI
- Runs entirely in the browser with no installation

## Tech Stack

- **HTML** — Structure and layout
- **CSS** — Styling
- **JavaScript** — Password generation logic

## Getting Started

1. Clone the repository:
   ```bash
   git clone https://github.com/iankala/password-generator.git
   ```

2. Open `index.html` in your browser — no installation required.

## How to Use

1. Open the app in your browser
2. Click **Generate** to create a new random password
3. Copy the password and use it wherever you need it

## Project Structure

```
password-generator/
├── index.html    # App layout
├── index.css     # Styles
└── main.js       # Password generation logic
```

## Author

**Ian Kala** — [GitHub](https://github.com/iankala)
